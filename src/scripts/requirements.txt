# pip install -r src/scripts/requirements.txt
#
# No browser needed: every MOTUS step is a plain JSON API call now, so
# Scrapling/Camoufox are no longer a dependency.
requests>=2.31
pdfplumber>=0.11
python-dotenv>=1.0
supabase>=2.0
