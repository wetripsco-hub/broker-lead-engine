# pip install -r src/scripts/requirements.txt
# then: scrapling install   (downloads the Camoufox browser StealthyFetcher needs)
scrapling>=0.4
requests>=2.31
pdfplumber>=0.11
python-dotenv>=1.0
supabase>=2.0
